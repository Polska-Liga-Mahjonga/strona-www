name: plm-cms
metadata:
  template: plm-cms@1.0.0

infra:
  provider: bicep
  path: infra

services:
  web:
    project: ./docs
    language: js
    host: staticwebapp

  api:
    project: ./api
    language: ts
    host: function
